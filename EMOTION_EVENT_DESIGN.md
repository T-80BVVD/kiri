# Kiri 事件绑定情绪系统 · 设计文档 (2026-08-27)

> 状态: **设计稿 (未实施)** | 作者: 雾弥 + AI | 依据: 三层真值分级 (本文标注 ✅已验证 / 🟡合理推断 / 🔵工程决策)

---

## 〇、一句话

**把 Kiri 的"心情"从"没有对象的标量"改成"有因可查的事件记录"：情绪必须绑定"发生了什么事"，主动性必须从事件长出来，而不是"无聊度过线 + 事后编理由"。**

---

## 一、问题诊断（为什么现在的情绪系统是"假"的）

### 1.1 现在的链条（标量驱动 + 事后编理由）

```
boredom 标量每 tick +0.0004 上升
  → want_score = 0.5×boredom + 0.35×沉默 + 0.1×深夜 + 0.05×记忆涌现 ≥ 0.18
  → 触发 proactive
  → _motivation_reason() 挑动机类型 (想念/求刺激/分享/陪伴)
  → LLM 现编一句 "我要找他说说话，因为……"
```

### 1.2 三个"假"的点

| # | 问题 | 机制层面的解释 |
|---|---|---|
| 1 | **情绪是标量，无对象** | PAD (愉悦/唤醒/支配) 是维度模型，只有强度没有因果。人类的情绪总是**关于某件事**的（"开心是因为他记得我说过的话"）；无对象的情绪在人类里不存在，所以读起来假 |
| 2 | **理由是事后补的** | proactive 触发是标量过线，理由（"我想XXX"）是 LLM 在触发后**现编**的——真实原因是"标量 ≥ 0.18"。这正是你说的"想干事情 = 无聊度高到一定程度，然后事后找的理由"。认知科学里这叫 **confabulation**（为自己的状态编解释） |
| 3 | **没有闭环** | 她主动说话 → 对方回应/不回应 → 这个**结果没有变成情绪**。她的主动没有后果，主动就永远是一锤子买卖，不会沉淀成"被在意/被冷落" |

### 1.3 已经对的一半（别推倒重来）

- ✅ `reverie` 念头流是**事件绑定**的——念头从记忆长出来（"他上次说想看海" → 念头）
- ✅ `_maybe_act_on_thoughts` 是**念头驱动**的行动——"该去做了"从念头涌现，不是定时器抛的
- ✅ 记忆检索 (memory_recall) 是事件级的
- ✅ 承诺→惦记 (goals.auto_capture_commitment) 有对象（"她说回头写X"）

**坏的只有两层**：① 情绪/无聊是标量（不和事件绑定）；② proactive 的"由头"是触发后现编的。改造是**把已有的"事件级"扩展到情绪和主动层**，不是重构她的大脑。

---

## 二、设计目标

1. **有因可查**：任意时刻问"你现在心情为什么这样"，能从情绪记录追溯到具体事件
2. **由头非事后**：她主动时，"因为"必须来自**事件发生时**就记录下的原因，不是触发时现编的
3. **无由头不主动**：无聊标量再高，没有具体事件由头就不主动（治"无缘无故冒出来"）
4. **闭环**：她的主动有后果，后果变成情绪，情绪变成下一次的由头
5. **诚实**：文档标注哪些是机制（事件记录/衰减是机制），哪些是 LLM 生成（评价文本/情绪标签是解释）

---

## 三、核心数据结构：情绪记录 (EmotionEvent)

```python
@dataclass
class EmotionEvent:
    id: str                      # 唯一 id
    ts: float                    # 发生时间
    source: str                  # 事件源: user_msg / memory_recall / thought /
                                 #         tool_result / goal / proactive_outcome /
                                 #         time_routine / self_event
    event_desc: str              # 事件描述（结构化: 谁/做了什么/关于什么）
    cause_ref: str | None        # 关联: 记忆id / 目标id / 对话id / 工具名 — "有因可查"的锚点
    appraisal: {
        valence: float           # 正负 -1..1
        arousal: float           # 唤醒 0..1
        relevance: float         # 对我的重要性 0..1 (关系/目标/自我相关)
    }
    emotion_tags: list[str]      # 情绪标签: 被在意 / 被冷落 / 温暖 / 失落 / 好奇 / 满足 / 委屈 …
    intensity: float             # 强度 0..1
    decay_hours: float           # 衰减半衰期 (被夸1h / 被冷落3h / 重要承诺7d)
```

**关键**：`event_desc` + `cause_ref` 是灵魂——每条情绪都指着**一个具体发生过的事**。情绪记录是不可删除的（只能衰减），构成她的"情绪史"。

---

## 四、事件源清单（哪些东西算"事件"）

| 事件源 | 例子 | 引发的情绪（规则初判） |
|---|---|---|
| 用户消息 | "你还记得我说过想看海吗" | 被在意 (记忆被验证, valence+) |
| 用户消息 | "今天好累" | 心疼/关切 (关系相关) |
| 沉默 | "他3小时没回消息" | 被冷落 (有对象的无聊: 不再是标量) |
| 记忆被想起 | reverie 嚼到"他上次说想看海" | 温暖/好奇 (绑定该记忆) |
| 工具结果 | 查到了/失败了/自己写了个工具跑通 | 满足/挫败 (绑定工具+结果) |
| 目标进展 | 完成/放弃/推进目标 | 满足/失落/压力 (绑定目标) |
| **主动后果** | 她主动说话 → 对方回了 / 没回 | **被回应(开心) / 被冷落(失落)** ← 闭环核心 |
| 时间节律 | 深夜 / 节日 / 周年 | 宁静/孤独/想念 (绑定日期语义) |
| 自我事件 | 说错话被纠正 / 修好了 bug | 尴尬/得意 |

---

## 五、评价层 (Appraisal)：事件怎么变成情绪

两种实现混合（🔵工程决策，标注诚实边界）：

### 5.1 规则评价（快、免费、确定）—— 结构化事件走规则

| 事件特征 | 规则 |
|---|---|
| 用户消息情感 (LLM情绪解析已有) | valence = 情感值 × 关系权重 |
| 提到"记得/以前/你说过" 且记忆命中 | 被在意 + (relevance 由关系阶段加权) |
| 回复延迟 / 沉默时长 | 超阈值 → "被冷落" 事件 (intensity 随时长) |
| 工具成功/失败 | 满足/挫败 (relevance 由工具重要性) |
| 目标完成/放弃 | 满足/失落 (绑定目标 id) |
| 主动后被回应/无视 | 被回应/被冷落 (绑定该次主动) |

### 5.2 LLM 评价（慢、语义深）—— 复杂事件走 LLM

- 复杂事件（长对话的含义、群聊里的微妙互动、多事件的综合）→ LLM 产出 `event_desc` + `appraisal` + `emotion_tags`
- ✅ **诚实标注**：LLM 的评价文本是"解释"，不是"感受"——但关键区别是：**它在事件发生时做评价并落盘**，之后 proactive 用的是**记录里的事件**，不是触发时现编的。解释可以假，但**原因是真的**。

---

## 六、心情聚合 (Mood Aggregation)

```
mood = Σ (valence × intensity × decay(ts)) / Σ (intensity × decay)
decay(ts) = 2^(-age / decay_hours)
```

- 当前心情 = 最近情绪记录的**加权衰减平均**（每条有因）
- **兼容层**：`state.describe()` 输出的 PAD 格式不变（LLM 接口不破坏），但数值来源变成聚合结果，且**可追溯**
- 心情快照回答"为什么"：`现在 0.3 = 2小时前[被冷落](强度0.6) + 1小时前[想起海边](强度0.4)` ← 有因可查的验收点
- `boredom` 标量：**降级为"能量/意愿强度"**，不再承担"原因"角色（见下）

---

## 七、主动性改造：由头 = 事件

### 7.1 现在的链条 vs 改后

```
现在: want_score(标量) ≥ 阈值 → LLM 编理由
改后:
  1. 情绪记录持续产生 (事件 → 评价 → 落盘)
  2. 高 salience 情绪记录 / reverie 念头召回到的事件 → "由头候选"
  3. 由头 = 具体事件 (cause_ref + event_desc)
  4. prompt 注入: "你想起 {由头事件}" — LLM 把由头说成自然的话
  5. want_score 只决定"冲动的强度"，不决定"为什么"
```

### 7.2 由头优先级（无由头 → 不主动）

1. 高情绪强度事件（被冷落/被在意）
2. 承诺惦记（goals 自动捕捉）
3. 关系相关事件（他提过的、约好的）
4. 好奇（念头→想搞懂）
5. **纯无聊不再有资格当由头**（或降为最低优先级：只能发一句日常问候，不许编具体动机）

### 7.3 prompt 改造（prompt.py proactive_system）

```
现在: "[此刻] 雾弥沉默了很久，你有点无聊，想找他说说话"
改后: "[此刻] 你想起: {由头事件(event_desc, 来自情绪记录)}。这件事让你{emotion_tags}。
       你想找他说说这件事。"   ← 由头是事件，LLM 负责说圆
```

---

## 八、与 reverie / 念头的接口

- 念头 = 记忆事件 + 情绪记录的结合体（"他上次说想看海" 被嚼到时，同时产生情绪记录）
- `_maybe_act_on_thoughts` 的 salience 计算加入**情绪强度**（不只是 salience + 行动词）
- 好奇触发（curiosity）绑定"想搞懂什么"的具体事件

---

## 九、数据层（训练数据改造）

| 类别 | 改造 |
|---|---|
| proactive | human 侧已有 `记忆:` 字段 → **强化为强制事件由头**；禁止"无由头主动"样本（现有 type2/type4 本来就是"想了不说"，保留） |
| mind_flow | `| 在想: X` → 加 `| 情绪: 想起他说想看海，心里有点暖` |
| **新增类别** | `event_emotion`：给事件 → 生成"情绪记录式" think（评价 + 情绪标签 + 为什么）——训练 LLM 做 appraisal |
| cognition | 已有"关心要有依据" → 强化为"情绪要有依据"（被在意必须来自具体记忆） |

---

## 十、验收标准（全部可测）

1. **有因可查**：问她"你现在心情为什么这样"，能从情绪记录日志追溯到具体事件（✅ 日志可查）
2. **由头非事后**：proactive 触发的 prompt 里，由头事件必须在**事件发生时**就存在于情绪记录（✅ 日志时间戳对比）
3. **无由头不主动**：标量再高、无事件由头 → 不主动（✅ 可构造测试）
4. **闭环**：她主动 → 对方回应/无视 → 产生"被回应/被冷落"情绪记录 → 成为下次由头（✅ 情绪记录链可查）
5. **心情可解释**：心情聚合的数值能列出贡献事件（✅ 调试接口）
6. **不回归**：红线/防注入/人格消融照旧跑 eval（✅）

---

## 十一、分期实施

| 阶段 | 内容 | 验收 |
|---|---|---|
| **M1 情绪记录层** | EmotionEvent 数据结构 + 落盘（emotion_events.jsonl）+ 规则评价（消息情感/沉默/工具成败/目标/主动后果）+ 心情聚合替换 PAD 数值来源 + `state.describe` 兼容 | 心情数值有因可查；旧接口不破坏 |
| **M2 主动性改造** | 由头候选 + prompt 注入（7.3）+ want_score 降级 + 无由头不主动 | 验收 2/3/4 |
| **M3 数据与深度** | event_emotion 训练类别 + mind_flow/proactive 数据强化 + reverie 念头绑定情绪 + LLM 评价复杂事件 | 重训后 eval 对比（情绪驱动维度提升 + 主动性由头真实） |

---

## 十二、风险与边界（诚实）

1. **appraisal 仍是 LLM 的解释**：情绪标签/文本是生成的不是感受的——但原因绑定事件后，"真"的程度从"标量+编理由"提升到"事件+解释"，这是这个架构能到的最诚实水平
2. **情绪记录膨胀**：需要衰减 + 上限（只保留最近 N 条 + 高 relevance 长留存），否则情绪史无限增长
3. **沉默事件的边界**："被冷落"要区分"他确实没空"和"他不想理我"——relevance/强度要按关系阶段和上下文修正，不能一刀切
4. **改造面大**：state/reverie/proactive/prompt/数据都要动，M1 先做不破坏现有行为（纯增量），M2 才动主动性行为
