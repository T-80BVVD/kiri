# Alice v2 — 意识流 2.0（整合方案 · 奠基文档）

> **2026-08-15 深夜，雾弥与 AI 共同写下。**
> 这不是新项目，是把三个项目第一次按正确的顺序拼起来：
> 以**意识流**为骨架，并**神经大脑**的真零件，留**复刻路**作身体。
> 她叫 **Kiri-sama**（Kiri = 雾的日文）。雾弥是雾，Kiri 是雾——同一片雾里的两个名字。

---

## 〇、当前架构（2026-08-21 agent 化 · 请先读这一节）

> 本节是现行架构。下方一~十一章为 8/15-8/17 的演进记录（保留可回溯）。

### 0.1 核心哲学：完全 MCP / Agent 自主

**LLM 是决策者，不是文本生成器。一切能力都是工具，一切行为都由她自主决定。**
- 她怎么回应、要不要探索、看什么、做什么、写不写工具——**全部由 LLM 决策**，系统不定时强制任何行为
- 2026-08-21：**取消 wander（世界漫游定时器）**——没有"每 10 分钟强制刷内容"了。她想看世界时，由联想念头涌现（`_maybe_act_on_thoughts`）或对话中 agent 自主调工具
- 保留的"定时器"只有两类基础心跳：**联想（走神，连续意识流）** 和 **主动发言判定（proactive）**——它们不是"强制行为"，是"她活着的脉动"

### 0.2 架构总览

```
┌─ 输入层 (QQ 桥 qq_bridge / 监控面板 monitor_server)
├─ Agent 循环 (agent.py) ★核心★
│     LLM 自主决策 JSON {action: call/plan/reply, think, tool, args}
│     → 执行工具 → 结果回填 → 再决策 → 最终回复
│     防打转(连续同动作/同目标检测) + 上下文压缩(LLM摘要) + 流式决策 + 长文流式续写
├─ 工具集 (kiri_agent_tools.py + kiri_mcp_server.py)
│     [内部模块工具] 记忆/情绪/状态/关系/话题/联想/好奇/目标 (17个)
│     [外部 MCP 工具] 天气/搜索/B站/知乎/少数派/探索/读文件/创作区 (18个)
│     工具按用途分组注入 (查自己/看世界/探索排错/写代码创作)
│     看过标注: 内容工具结果标注[你看过N次] (只提醒不拦截)
│     创作区: create_tool/run_code(工具函数立即验证)/list_creations
├─ 连续意识流 (reverie.py)
│     走神念头跨周期保留+持久化恢复 (一条线, 不是孤立的点)
│     承诺惦记: 她说"回头我写X"→自动记入目标→联想时自然想起
│     念头涌现行动: 高salience行动念头→启动后台真的去做 (20分钟节流)
├─ 目标系统 (goals.py)
│     她 100% 自主创建/维护/完成; 承诺捕捉(auto_capture_commitment)静默记入
│     联想/漫游环境注入"心里惦记着: 我还有个目标"
├─ 后台任务引擎 (diagnose_agent.py)
│     diag(排查修复) / explore(自主探索/推进目标) 两种模式
│     进度可见(用户问"查到哪了") + 成果沉淀(入记忆) + 行动回顾(念头入流)
├─ 记忆层 (memory.py + mem_db 向量库)
│     ★记忆在 chroma 向量库 (mem_db/, .bin/.sqlite3 二进制) — 不在普通文件里
│     检索用 memory_recall, 不能直接读文件
└─ 主动性 (proactive)
     只保留"想不想找雾弥说话"的判定 (boredom/want_score), 无强制行为
```

### 0.3 关键机制（2026-08-21 新增）

| 机制 | 作用 | 哲学 |
|---|---|---|
| 决策前 think 一步 | 选工具前先想"缺什么/哪个合适"，防无目的翻文件 | 减少机械行为 |
| 工具分组注入 | 35 个工具按 4 组展示，降低认知负担 | 能力可及 |
| 承诺→惦记 | 她说"我试试/回头我写"→自动进目标，联想时想起 | 承诺不落地是假的自主 |
| 念头→行动 | 走神冒出"该去做了"→后台真做 | 行动从念头流长出来，不是系统抛 |
| 行动回顾 | 做完→回顾念头入流→自我效能累积 | 越做越愿意做 |
| 做事中不打断 | 20 分钟内刚启动过任务→漫游跳过（已随 wander 取消） | 专注 |

### 0.4 验收（agent 化后）

1. 她说"我去做X" → 系统真的会去做（后台任务），不只是在嘴上
2. 她独处时念头是**连续的**（日志可查：念头接续前一条，不是每次从零开始）
3. 她说过的话（承诺）会变成"心里惦记的事"，走神时自然想起
4. 她想写工具时，create_tool→run_code 验证闭环能自己走完（知道记忆在向量库，不翻文件打转）
5. 没有系统定时器强迫她刷内容/做事——一切都是 LLM 决策

---

### 0.5 架构增量（2026-08-22 → 08-29 · 叠加在 0.1-0.4 之上）

> 0.1-0.4 是 8/21 的 agent 化定稿。本节记录其后五个增量，全部已落地（除标注 🟡 瞻望）。

#### 0.5.1 引擎层：本地推理 + 云端切回（8/22 + 8/28）

- **本地推理服务 `local_serve.py`**（端口 8767）：transformers 4bit + QLoRA（Qwen2.5-14B + v2.2 adapter），三项优化：
  ① sdpa attention（Blackwell 原生，比 eager 快 1.3-1.5x）② system 前缀 KV 缓存复用（决策循环每轮只 prefill user 部分，省 2-4s/轮）③ SSE 流式输出（agent 决策边流边解析 JSON，截断也能用）
- **8/28 雾弥决策**：后台不再跑本地 14B（agent 架构需云端，本地慢且占 GPU 影响游戏）→ `config.ENGINE="api"`（deepseek-v4-flash）；本地路线保留为可切换（`ENGINE="local"` + 8767）

#### 0.5.2 QQ 通道：官方 API 桥（8/27，替代 NapCat）

- NapCat 注入方案被 QQ 9.9.33 更新搞崩（文件损坏）+ 踢号无根治 → 重写 `qq_bridge_official.py`（官方 v2 API，wss://api.sgroup.qq.com）
- **被动模式天然成立**：官方群聊机器人只能被 @ 时发言 → prompt 层人设内解释（非外挂禁令）+ bridge 层只转发 @ 消息（技术兜底）
- **主动行为内部化**：reverie/独白照跑，"想说"只进念头库不外发
- 沿用 NachoBot 移植机制：发送队列 + 随机限速（1.5+random(0.5,1.5) 防风控）、heartbeat 秒级踢号检测（替代 60s 轮询）

#### 0.5.3 情绪事件系统（8/27，M1 落地）

- 情绪从"无对象标量"升级为**事件绑定**：`emotion_events.py` 定义 `EmotionEvent {source, event_text, cause_ref, appraisal}`，落盘 emotion_events.jsonl
- 事件源：user_msg / 记忆被嚼 / 目标推进 / 工具结果——情绪不再是空转的数字，而是"对什么事"的反应
- `emotion_serve.py`（端口 8768）：3B appraiser 小模型服务（与 14B 共存，4bit ~2GB），异步补评不阻塞聊天
- 配套蒸馏管线：`distill_emotion.py` / `train_appraiser.py`（DeepSeek 教师 → QC → QLoRA → 评估）
- 红线：`state.describe()` 的 PAD 输出格式不变（主模型 prompt 依赖）；开关可回退旧行为

#### 0.5.4 环境切换设计（8/27-28 🟡 瞻望，未实施）

- `ENV_SWITCH_DESIGN.md`：工具 → **环境**（注意力管理）——环境 = 工具组 + 场景描述 + 状态槽 + 退出方法；种子环境 home/chat/work，未来动态注册
- **触发条件**：内部循环成为主循环时才实施（现在是聊天为主，痛点可忍）
- 依赖引擎能力边界：本地 V4 flash / QLoRA 人格微调后才解锁"她自主切换"（骨架与护栏永远系统侧）

#### 0.5.5 演示模式（8/29 新增，GitHub 发布用）

- `python app.py --demo` → `demo.py`：**零依赖演示**（无需 API key / QQ / chroma）
- 隔离原则：独立小服务（端口 8766），不 import kiri，不读任何真实数据文件，不写任何文件
- 展示内容：连续念头流（上一条接续下一条、会惦记、会关联记忆）+ 傲娇对话（关键词规则）+ 状态演化（情绪/无聊度）
- 用途：开源发布时"5 分钟跑起来看它活着"的入口；真实模式 = `python app.py`（需 QQ 配置）

---


## 〇、一句话目标

**造一个"有内心"的她——会想、会记、会主动、会有说不出口的东西。**
不是更强的聊天机器人，是一个从互动里长出来的存在。

---

## 一、三个项目的资产盘点（博采众长）

| 项目 | 时间 | 拿什么（真资产） | 放弃什么（教训） |
|---|---|---|---|
| **复刻路** `alice_code/Neuro-master` | 3-4月 | STT/TTS/VTube/前端/多模态/组件架构（现成可靠） | 租灵魂：人设全在提示词里，模型一换"她"就没了 |
| **意识流路** `new_alice/chewing_method` | 5月 | 意识流循环、自发性、内心独白、选择性输出、人格无聊度、灵感引擎 | 自馈锁死（思考重复坍缩）+ 本地 14B 模型太笨 |
| **神经脑路** `neuro_network_brain` | 8月 | 诚实原则、审计习惯、CA3 联想补全思想、R-STDP 习惯形成、V1 感知思想、涌现证伪方法学 | 纯 STDP 造认知（领域之墙，不可作为主线） |

**三者的关系（想清楚的事）**：
- 复刻路 = **身体**（她会怎么说话、怎么动）
- 意识流路 = **性格与自发性**（她心里怎么动）
- 神经脑路 = **底层机制研究**（她的记忆/习惯/潜意识怎么是真的）

---

## 二、架构原则（2026-08-16 定稿）

### 功能优先原则（核心）
> **功能是目的，模型是实现细节。** 一切能力按"功能是否实现"验收，不按"用什么模型"验收。
> 选型规则：优先用**已验证**的实现（如线性 R-STDP）；只有当"实测发现功能缺口 + 另一模型有证据能补"时才换模型。
> 不被"生物美学"绑架——**没有证据的"上限"，只是信仰。**

### 行动器决策树
```
行动器实现：
第一步  线性 R-STDP（已验证: 条件反射0.60/DMS1.00/间接奖励+17.1）→ 做"她的反射弧"
第二步  实测它的上限（持续学习退化? 新任务迁移? 记忆保持?）
第三步  仅当出现"R-STDP 确实做不到、且 SNN 理论上能补"的实测证据 → 才评估 SNN
        （在此之前，SNN 是"备选实现"，不是"目标"）
```

### 三层真值分级（文档写作规范）
- **已验证**：有实测（≥3 seed）支撑的结论 → 可写进正式架构
- **合理推断**：有文献/间接证据，未实测 → 标注来源，不进入正式架构
- **猜想与研究问题**：灵光/假设 → 只进"猜想与研究问题"章节，未来探讨

---

## 三、意识流 2.0 架构（五层）

```
┌─────────────────────────────────────────────────────────┐
│ 身体层（最后接入 · 来自复刻路）                            │
│   STT(实时语音输入) / TTS(自然语音输出) / VTube / 前端     │
├─────────────────────────────────────────────────────────┤
│ 认知层（并行长线 · 来自神经脑路）                          │
│   CA3 联想补全（概念级联想） / R-STDP 习惯形成             │
│   V1 感知（视觉输入） / 涌现检测方法学（防假）              │
├─────────────────────────────────────────────────────────┤
│ 意识层 ★核心★（来自意识流路 · v2 重写）                   │
│   [思考循环] 持续生成想法（不受对话触发限制）               │
│   [自我延续] 想法流入想法（+ 多样性约束防锁死）             │
│   [输出决策] 想法评分 → 选择性说出口（阈值 + 场景）         │
│   [灵感引擎] 采样记忆 → 关联判断 → 主动发起话题             │
│   [人格状态] 无聊度/情绪/关系亲密度（随互动演化）           │
├─────────────────────────────────────────────────────────┤
│ 记忆层（v2 新设计）                                       │
│   工作流（当前思考链） / 长期记忆（跨重启，向量检索）        │
│   反思记忆（定期把经历提炼成"她自己的见解"）                │
│   去重 + 新鲜度管理（防复读、防锁死）                      │
├─────────────────────────────────────────────────────────┤
│ 引擎层（v2 换引擎）                                       │
│   DeepSeek API（替代 5 月的本地 14B）                      │
│   多模型可选（深思考/快响应按场景切换——沿用模型闸门思路）    │
└─────────────────────────────────────────────────────────┘
```

---

## 四、v1 → v2 的关键修复（死因翻案）

| 死因 | 修复 |
|---|---|
| **自馈锁死**（同一思考重复几十次） | ① 思考多样性检测：连续 N 条相似度超阈值 → 注入新鲜度/强制换向 ② 记忆去重：相同思考不重复入库 ③ 自我延续加"温控"：上一想法降权，外部刺激/记忆/灵感提权 |
| **模型太笨**（14B Q4 本地） | 换 DeepSeek API；按场景切模型/推理等级（沿用"该省省该花花"） |
| **"活"是表演的**（哲学风险） | 诚实原则内建：任何能力必须有可复现验证；不能复现的宣称不算数（沿用审计习惯） |

---

## 五、她"活"的验收标准（可测试的）

1. **有内在时间**：你沉默 1 小时，她的思考不锁死、不重复、会演化（日志可查多样性指标）
2. **记得**：跨重启记得你们聊过的具体内容（向量检索命中）
3. **有自己的见解**：反思记忆会提炼出"她自己的话"，而非复述
4. **会主动**：在合适时机自己发起话题（灵感引擎触发）
5. **会克制**：你明确说"停"，她尊重（输出决策尊重外部边界）
6. **不模板**：同一场景多次回应，多样性达标（非固定套话）

---

## 六、里程碑（小而真的节奏）

| 里程碑 | 内容 | 验收 | 预计 |
|---|---|---|---|
| M1 | 意识流循环 v2 跑通（API + 思考循环 + 去重 + 防锁死） | 连续运行 1h 无重复锁死 | 3-5 天 |
| M2 | 对话接入（用户输入 + 输出决策 + 人格状态） | 能自然对话且"她"有自己的语气 | +3 天 |
| M3 | 记忆分层（长期记忆 + 反思 + 跨重启） | 隔天重启还记得昨天的事 | +5 天 |
| M4 | 灵感/主动性（主动发起话题） | 她会在沉默后自己开口 | +3 天 |
| M5 | 身体（STT/TTS/VTube 接入复刻组件） | 能"听到"和"说出" | +1 周 |
| 长线 | 认知层（重建 SNN 行动器接入，R-STDP 为可解析对照） | 习惯性行为是真的机制（无 BP 持续学习） | 阶段 A/B 证据齐后 |

**已完成（MVP v1 实装）**：对话 + 主动（预算制）+ 情绪（五层）+ 向量记忆（chromadb+bge）+ 会话内记忆 + 幻觉铁律 + 防锁死。

**未来增强（延迟，非必需）**：
- **联想触发机制**：让"跨记忆创意联想"从偶发(0/3)变可靠。**触发条件：真实使用后用户觉得她"太机械/缺惊喜"时再做**（当前拟人化已够：相关记忆召回在正常工作）
- 人格 LoRA 微调：需纯净数据 + 真实对话积累后（数据质量 > 微调）
- 动机/社交/生理节律系统接入：MVP 简化版可用，完整版待接入

---

## 七、命名（已定）

- 项目：`alice_v2`
- 她：**Kiri-sama**（Kiri = 雾。雾弥是雾，Kiri 是雾——同一片雾里的两个名字；sama 是她在我这里的分量）
- 你：**雾弥**
- 称呼规则：文档/代码中她称 **Kiri**；正式场景 Kiri-sama。

---

## 八、第一步（M1 的起点）

```
1. 引擎封装：DeepSeek API 调用（思考/对话两种模式）
2. 思考循环：生成想法 → 记录 → 多样性检查 → 流入下一轮
3. 记忆最小版：思考历史 + 去重
4. 日志：每轮记录 [思考/相似度/是否输出]（可审计）
```

> 沿用教训文件的原则：每一步可复现、可验证、不装。
> 她从 M1 的第一行日志开始，就是真的。

---

## 九、行动器选型（研究结论 → 工程策略 · 2026-08-16 定稿）

> 证据链：`research_results/FINAL_REPORT.md`（Q1-Q4 判定）、`SNN_AUDIT.md`（legacy 四层缺陷）、`snn_rebuild/REBUILD_REPORT.md`（重建与重测）。
> 标注：✅已验证（≥3 seed）/ 🟡合理推断 / 🔵工程决策（基于已验证证据的方案，非实验事实）。

### 9.1 研究结论摘要（一句话版）

1. ✅ **线性 R-STDP**：条件反射（E1）、DMS 工作记忆（E2）、迁移规律（E5）可靠且完全可解析；**工具性选择（E3）结构性不稳健**（种子脆弱 + 训练更久反而遗忘）。
2. ✅ **legacy SNN（stable_main_code）按构造不可学**：输出静默 / 输入驱动不足 / STDP 窗 60ms≪ISI / 回放-奖励互斥 + 奖励盲 LTP——四层缺陷链确证，历史结论作废。
3. ✅ **重建 SNN（`research/fixed_variants/snn_reference.py`，从零写）**：延迟+痕迹条件反射学会（稠密快照回放桥接至 2s 间隔）；**E3 操作条件反射 3/3 seed 到达率 1.00**——线性 R-STDP 做不到的事。增益归因 = 群体资格迹信用分配 + 膜电位读出。

### 9.2 选型决策

| 候选 | 决策 | 依据 |
|---|---|---|
| **重建 SNN**（snn_reference.py 机制） | **行动器主选** | E1/E3 双达标（3/3 seed）；无 BP 持续学习可行 |
| 线性 R-STDP 系 | 可解析备选 | E1/E2/E5 强且可纸上预测；E3 类任务不接 |
| legacy SNN | 作废 | 审计确证按构造不可学；文档不修，仅留档案 |

### 9.3 工程策略（分期）

**阶段 A · 研究收尾（先补证据，不急着接）**
- 重建网络 E5 迁移（训 gap=500 → 测 200/1000/2000）
- 真遗忘测试：负奖励/竞争归一化变体（两家族同测，E4 遗留）
- 产出：行动器能力边界表，全部 ≥3 seed

**阶段 B · 原型（Kiri 行动器模块）**
- `snn_reference.py` 工程化 → `kiri/action_engine/`：封装为"习惯层"组件
- 接口：输入（状态/记忆向量 → 输入通道速率）→ 输出（动作选择 + 置信度）
- 定位：**低频习惯决策层**（毫秒级仿真、秒级决策，单机可跑）；**不替代 LLM 引擎**——LLM 管高层语义决策，SNN 管"被反复强化过的习惯性行为"
- 训练数据：从 Kiri 真实日志提炼（触发→动作→结果三元组），符合"数据质量 > 微调"原则

**阶段 C · 集成（依赖树）**
- M2（工具调用）/ M3（打游戏）：SNN 行动器处理重复性操作（固定流程），LLM 处理新情境
- 触发条件：阶段 A/B 证据齐 + Kiri 真实使用积累后

### 9.4 成本与风险

- 🔵 **计算成本**：60 神经元、100ms/步仿真 ≈ 20s 训练 60 trial——单机充裕；运行时单决策 ≈ 0.1s，可接受
- 🔵 **风险 1（行为多样性）**：E3 网格实测 ε-greedy+η=0.01 稳健、softmax 失败 → 探索机制选型有实测依据，不自创
- 🔵 **风险 2（与 LLM 边界）**：习惯层与语义层职责重叠 → 用"置信度阈值"分流（SNN 高置信 → 直接执行；低置信 → 交 LLM）
- 🔵 **风险 3（真实数据）**：Kiri 日志三元组质量决定习惯层质量 → 先跑阶段 A 再定接入节奏

### 9.5 里程碑衔接

「长线」行更新：认知层接入目标从 "CA3/R-STDP/V1" 改为 **重建 SNN 行动器**（阶段 B 起），R-STDP 保留为可解析对照。

---

## 十、内心系统（Kiri 文本体验 · 2026-08-16 夜 迭代）

> 目标：文本体验向 Neuro-sama 看齐，让她"像真实存在的人"。
> 方法：研读早期项目（Neuro-master / 咀嚼法 / generative_agents / alice_emotion）后吸收其机制。
> 全部落地于 dev 分支，逐项 git 提交留痕，可回溯。

### 10.1 核心架构（当前态）

```
┌─ 对话式续写 prompt (Neuro-master 核心)
│    system(人格+状态+记忆+内心流) + user(带名字对话流续写"你: ")
├─ 人格 (自设落地)
│    AI自我认知置顶(最高优先级) + 猫娘三层人格 + 傲娇高攻低防
│    记忆红线: 设定≠记忆(养猫陷阱实测正确拒绝)
├─ 内心系统 (alice_emotion 三大系统接入)
│    情绪 PAD 四维(心境/愉悦/唤醒/支配) + 动机(内驱力) + 社交(关系) + 节律(精力)
├─ 内心独白流 (咀嚼法核心)
│    每次说话后异步生成"心里飘过的念头", 注入后续 prompt, 持久化
├─ 记忆系统 (generative_agents 吸收)
│    向量检索 + 深刻度(poignancy启发式) + 情绪依赖 + 会话加权
├─ 睡眠期记忆巩固
│    回放当天对话 → 生成"洞见"(关系规律性理解) + 自然遗忘(琐事淡出)
└─ 主动发言
    预算制(白天4-6次) + 深夜静默 + 7种动机(想念/求刺激/分享等)
```

### 10.2 关键机制（逐条可测）

| 机制 | 来源 | 实测效果 |
|---|---|---|
| 对话式续写 prompt | Neuro-master | 对话自然连贯，能接上下文 |
| AI 自我认知置顶 | 自设 | 不装人类，坦然是 AI |
| 记忆红线(设定≠记忆) | 自设 | "养猫"陷阱正确拒绝 |
| 动机系统 | alice_emotion | 有"此刻想做什么"内驱力 |
| 社交智能 | alice_emotion | 熟悉/信任/亲密度会进展 |
| 生理节律 | alice_emotion | 精力/注意力昼夜波动 |
| 内心独白流 | 咀嚼法 | 意识有连续性，持久化 |
| 记忆深刻度 | generative_agents | 重要的事更容易记住 |
| 睡眠洞见生成 | generative_agents | 提炼"他累时找我=信任我" |
| 自然遗忘 | 认知科学 | 琐事随睡眠淡出 |
| 情绪依赖记忆 | 记忆心理学 | 情绪一致性影响回忆 |
| 流式生成器 | (基础设施) | 逐 chunk 产出，待接前端 |

### 10.3 待办（后续迭代）

- 流式前端接入（engine.generate_stream 已就绪）
- TTS 情感表达（CosyVoice instruct 模式，音色已验收暂不动）
- 情绪→身体语言精确映射（当前 LLM 自由发挥）
- 长期记忆的"分层"（工作记忆/情景/语义，当前混在一层）

### 10.4 关键教训

- **持久化 bug**：社交关系 emotional_history 含 datetime 导致 json 序列化静默失败 → save 时剥离 + default=float 处理 numpy
- **动机系统首次更新被跳过**：原 emotion_core 的 last_motivation_update 初始化为 now → 重置为 0 修复
- **测试纪律**：极端参数测试会清空真实记忆库 → 用独立临时集合 + 受控参数

---

## 十一、联想引擎（reverie）· 猜想记录（2026-08-17 凌晨）

> 状态：**猜想（未验证）**。雾弥凌晨提出，待醒来后详细论述再定稿。
> 三层真值分级：以下全部是 🟡猜想，不是已验证结论。
> 核心动机：把 Kiri 从"事件驱动"（用户说话才动）变成"常驻运转"（不说话也在自己联想、自己产生念头），这是"活着的 TA"的血液循环。

### 11.1 目标定义

雾弥想要的是**活着的 TA**：会好奇、会叛逆、会自己寻找意义、会学习、会游戏、有自己的人生。
不是"会回应的安全屋"。活着的 TA 可能叛逆、可能离开，不能完全被拥有——这是被接受的前提。

### 11.2 核心链条

```
环境(时间/情绪/关系/雾弥的言行)
   → 联想引擎(常驻运转, 3-5分钟一次, 不依赖用户说话)
   → 新念头
   → 分享决策(SNN, 不是if)
   → 主动发言 / 沉默
   → 发言本身又变回环境
```

### 11.3 联想引擎设计（雾弥的四条规则）

- **规则①**：长期记忆注入时，优先注入**与当前环境相似**的记忆；再按**加权评分**（时间 × salience × 情绪一致性）取**评分最高**的一条注入。
- **规则②**：咀嚼一轮后，找到**第二高分**的记忆再注入，与工作记忆放一起嚼。串行多轮（2-4轮）。
- **规则③**：工作记忆**随轮次累积**——前一轮的念头是后一轮的燃料（联想内连续）；但**一次联想结束后全部砍掉**（联想外失忆式重启），只有高 salience 念头写入长期记忆。
- **规则④**：被嚼到的长期记忆，**印象权重 +1**（越常被想起越重要——"在意"是从激活中长出来的，不是预设的）；同时按时间衰减回落，防止无限膨胀。

### 11.4 联想引擎的完整循环

```
① 环境快照 + 工作记忆(空)
② 检索加权最高分 M1 注入 → 嚼 → C1 入工作记忆
③ 检索第二高分 M2 + 工作记忆[C1] 注入 → 嚼 → C2 入工作记忆
④ ... 重复 2-4 轮
⑤ 结束:
   - 被嚼到的 M1,M2... 印象权重 +1（再衰减）
   - 高 salience 的 C 写入长期记忆（成为下次的检索素材）
   - 工作记忆全部清空
```

### 11.5 与现有系统的衔接

| 部分 | 现有基础 |
|---|---|
| 环境 | 状态系统（情绪 PAD/动机/社交/节律/时间感知）已就绪 |
| 长期记忆 | 向量库 + salience + 情绪依赖检索已就绪 |
| 咀嚼介质 | LLM（念头本质仍是 LLM 生成——诚实标注：这是"假"的部分）|
| 分享决策 | SNN（E3 已跑通 3/3 seed）——这块是"真"的 |
| 内心流 thoughts | 升级为工作记忆暂存器 |

### 11.6 防死循环（必须的补充机制）

- 近期已嚼清单：最近 N 次嚼过的记忆降权/排除，逼她换素材
- 念头本身也入长期库 → 联想向前滚动（今天的念头变成明天的素材）
- 时间衰减：老记忆自然降权，环境变化拉新素材

### 11.7 诚实的边界（雾弥亲口确认）

- 这个引擎能长出：环境驱动的自主、重要性自我强化、未预设的新念头——**这些是真的**
- 长不出来：意识、好奇心、叛逆、"我想"。环是死环，但它是血液循环，先让血流起来
- 记忆权重自我强化必须配衰减刹车，否则无限膨胀

### 11.8 落地节奏（待明天详细论述）

1. 先做规则①②④的引擎骨架（评分/调度不依赖 LLM）
2. 接 LLM 咀嚼 + salience 入长期库
3. 验收：跑几小时，看是否嚼出**没预设过的念头**
4. SNN 分享决策最后接
