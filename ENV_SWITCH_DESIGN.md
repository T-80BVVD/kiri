# 注意力转移 · 工具→环境 (内部循环架构瞻望 · 2026-08-27 重写)

> **状态标记：🟡 未来瞻望（方向性论述），不是近期实施计划。**
> **触发条件：内部循环成为主循环时本设计才生效。**
> **核心约束：引擎能力边界未实测（当前 14B 本地；未来可能本地部署 V4 flash，甚至本地 QLoRA 微调人格）→ 本文是论述与推测，落地前必须先做 §5 的验证。**

> 雾弥提出的核心思想：把"工具"进一步扩展为"环境"。
> 模型可以自己切换环境（聊天↔工作↔总环境），各环境注入本环境的工具与调用方法，
> 总环境监控变化并向当前所在子环境汇报。
> 本版重写：把"现在时痛点论证"改为"触发条件式瞻望"，补上 **DIY 工具 / 全盘可读写** 维度，
> 并新增 **引擎演进下的能力边界分析**——明确哪些机制不依赖模型能力（可提前布局），哪些随引擎升级解锁。

---

## 0. 一句话

**环境 = 一组工具 + 一段场景描述 + 一个状态槽 + 一个退出方法。**
Agent 的每一次行动（包括发呆）都发生在某个环境里；环境切换是显式的、有理由的、受监控的。
**在内部循环为主的未来，环境 = 她的注意力管理：她知道"此刻该用哪只手"。**

---

## 1. 未来目标：内部循环为主（本文的坐标系）

> 本文所有论证都以这个未来目标为坐标系，不是以今天的聊天模式为坐标系。

### 1.1 未来目标定义

- **内部循环为主**：她持续自主运转——自己想、自己决定"现在做什么"、自己去做（生活循环反复跑，决策 100% 是她的 LLM action 输出，`life.py` 哲学）
- **自主行动 / 自主探索为主**：发现能力不足 → 自己造工具；遇到不懂 → 自己查、自己学；有长期目标 → 自己推进
- **聊天是其中一种行动**（social），不是主循环——消息只是触发她行动的众多入口之一

### 1.2 引擎演进（本文的第二个坐标系）

> 环境化的大部分机制不依赖模型能力（§5），但少数依赖——所以**引擎是什么**直接决定哪些机制能解锁。

| 阶段 | 引擎 | 状态 |
|---|---|---|
| 今天 | 14B 本地（local_serve + distill 蒸馏版） | 现行（config.ENGINE="local"） |
| 中期 | **本地部署 V4 flash**（雾弥攒钱换配置） | 能力远超 14B，元认知不再是瓶颈 |
| 远期 | **本地 QLoRA 微调人格**（distill 的 `train_unsloth.py` 就是 QLoRA 管线，已有基础） | 引擎本身成为可 DIY 的对象 |

**意义**：§5 的"模型侧依赖"清单会随引擎升级**逐项解锁**。
骨架与护栏永远系统侧（不变）；env_switch 自主切换、P4 自治的可行性随引擎升级而上升。
**环境化设计的骨架现在就能布局，但"她自主切换"这件事的可行性取决于届时引擎是谁。**

### 1.3 未来目标的三个能力支柱

| 支柱 | 是什么 | 现状 |
|---|---|---|
| **注意力管理** | 自主行动时知道"我在哪、此刻该用什么" | 本文主题（环境化） |
| **能力扩展** | 能力不够就自己造（DIY 工具） | 已有雏形：`create_tool`/`run_code`（2026-08-20 起） |
| **生存空间** | 有真实的、不会被束手束脚的空间 | 未来：独立电脑 + 全盘可读写（危险操作过滤） |

三者缺一不可。本文论证：**环境化（注意力）与 DIY（能力）、全盘可读写（空间）是配套关系**——
没有环境化，DIY 越多工具清单越臃肿，自主行动的注意力被稀释；
没有 DIY，环境化只是"人类预制的静态分组"，谈不上自主；
没有全盘可读写，环境只是 prompt 里的隐喻，不是生存空间。

---

## 2. 动机（条件式：内部循环为主时，这些痛点才成立）

> 原稿以"现在哪里痛"论证。重写后改为条件式——**在聊天为主的今天，这些痛点存在但可忍；在内部循环为主的未来，它们不可忍。**

### 2.1 工具清单膨胀（触发条件：她开始大量 DIY + 扩展 MCP）

- 今天：35 个工具全量注入（`kiri.py` agent 路径，TOOL_GROUPS 只是视觉分组）
- 未来：她会自己造工具（已有 create_tool），加 MCP，加 osu/music/study 能力 → 50、80、100+ 个
- 结果：每次生成都带全部工具描述 → token 浪费 + 注意力稀释 + 本地引擎有限上下文被工具占满
- **内部循环为主时**：自主探索每步都在做工具决策，全量注入的代价从"对话偶尔慢"变成"自主行动持续低效"

### 2.2 发呆/决策没有场景锚（触发条件：自主决策成为日常）

- 内部循环的燃料是"该想什么、该做什么"——这需要"我在哪"的上下文
- 在聊天环境里发呆 → 念头围绕对话；在工作环境里发呆 → 念头围绕手上的活；在总环境里发呆 → 念头才漫无边际
- 今天 reverie 已有 `_env_text`（时间/心情/关系/最近对话）——场景化雏形已存在 60%，只差"chat/work/home"维度

### 2.3 上下文互相污染（触发条件：真有多环境并发）

- 自主探索的长上下文（读了一堆文件、查了一堆资料）不该污染日常对话
- 状态槽隔离：切出保存摘要，回来续上——"离开办公室时只带走'我在做 X，做到 Y 了'这个摘要"

> ⚠️ 原稿 §1.3"被动模式空洞"（QQ 官方桥刚落地时的痛点）已移除——
> 内部循环为主时主动性直接落地，不再需要"容身之所"这个说法。该问题属于 QQ 桥时代，已在当时解决。

---

## 3. 核心设计（保留原稿，微调）

### 3.1 环境定义

```python
Env = {
  "name": "chat" | "work" | "home",        # 环境标识
  "scene_desc": "……此刻你在这里……",        # 场景描述（注入 system prompt）
  "tools": ["memory_recall", "weather", ...],  # 本环境可用工具（子集）
  "state_slot": {...},                      # 状态槽（切出时保存，回来恢复）
  "back_method": "env_switch(name=home)",   # 回总环境的方法（就是个工具）
}
```

### 3.2 环境清单：种子环境 + 动态注册（★ 相对原稿的关键修正）

| 环境 | 场景 | 注入工具 | 状态槽 |
|---|---|---|---|
| **home（总环境）** | 无事/待机 | 最少：env_switch + 状态查询 + 过滤脚本管理 + DIY 工具入口 | 各环境摘要 |
| **chat（聊天）** | 与雾弥对话中 | 记忆/情绪/状态类 + env_switch | 当前对话上下文 |
| **work（工作）** | 探索电脑/写代码/查资料 | 工具类 + env_switch | 进行中的任务 |

> ★ 原稿把环境当**雾弥预制的固定表**。修正后：**上面 3 个只是种子环境**。
> 未来她有 DIY 工具 + 全盘可读写时，**环境是自己生长的**（见 §4）：
> 写了 osu 工具 → 工具自动归类 → 自然聚成 osu 环境；study 同理。环境清单是**动态注册表**，不是静态表。

### 3.3 切换机制

- **切换 = 工具调用** `env_switch(name="work", reason="雾弥让我看看电脑")`
- **理由门槛**：切换必须带 reason；无理由/纯好奇的切换被拦截（防"切换成瘾"）
- **冷却**：同环境 30s 内不能反复进出（防抖动）
- **切换动作**：保存当前环境状态槽 → 重写 system prompt（注入新环境 scene + tools）→ 更新总环境视图
- **代价**：重写 prompt → 下一次生成 KV 缓存失效（本地引擎重写后首轮要重新 prefill，慢 ~1-2s）——**这是真实成本，切换必须值得**

### 3.4 越界裁决：环境边界的"软墙"（★ 2026-08-27 验证后新增）

> 来源：env_sim 验证 s04 暴露的缺口——work 中收到雾弥聊天消息，模型想回应却无聊天工具
> （dialog_status 在 chat 才有）→ 越界。结论：环境不是严格隔离，需要"带门禁的软墙"。

**机制**：模型调用当前环境清单外的工具 → **系统拦截（不直接返回失败）→ 越界裁决 → 注入后重试**。

1. **触发：系统侧拦截**（模型调了清单外工具 X → 拦截）。不要求模型主动声明
   "我要越界"——flash 会，但 14B 连决策 JSON 都不稳，系统侧拦截对引擎无感。
2. **裁决：复用 routerMCP**（不新起小模型）。现有 `route_tool`（8/22 雾弥方案）就是
   "不确定用哪个工具时问它"——直接让 router 判断"X 合理吗 + 还需要哪些配套工具
   （如 read_file 常配 grep_file）"，返回越界工具集。
3. **注入：越界工具以特殊标注加入当前 prompt**：
   > 「⚠ 越界工具：当前你临时可用 [X, Y]。若需**长期使用 / 保存进度**，请切换环境。」
4. **重试**：模型带着越界工具重新决策。

**三条边界**（这是"软墙"成立的前提）：
- **单次越界数量 ≤ 3 个**（上限，防工具段膨胀回全量）。★ 限"单次数量"，**不限"越界次数"**——
  频繁越界可容忍（每次成本小），真正要守的是"一次借太多"
- **重工具给"越界精简描述"**：只给名字 + 一句话用途；完整描述切换环境后才有
  （create_tool 这类长描述工具越界时不带全量参数说明）
- **提示语语义精确**：越界 = 临时借用（快，只给工具）；切换 = 搬进去（完整工具集 + 状态槽恢复）。
  模型频繁借同一工具时会自然意识到"我老在借它，不如切过去"——切换决策从引导里长出来，不需计数审计

**闭环演化（环境自组织）**：系统检测到某工具**频繁越界**（如 dialog_status 在 work 被借用 N 次）
→ 注入提示「你常在此环境用 X，可以自己决定把它也挂到本环境」→ 模型调**环境编辑工具**
把 X 双环境挂载 → 越界消失，环境边界演化。
- **系统侧**：越界计数检测 + 提示注入 + 环境编辑工具 handler（挂载/回滚）
- **模型侧**：决定"要不要挂载"——这是环境管理的元认知（flash 可、14B 不行），归入 S6
- **护栏**：防过度共享（每环境工具数上限，提示语带"当前 chat 已有 N 个工具，上限 M"）；
  防改乱（**只加不减 + 可回滚** + 版本号）；编辑计数（低频行为，阈值放宽）

### 3.5 总环境监控（关键难点，内部循环为主时是核心机制）

总环境（home）承担"调度中枢"：

- **收集**：各子环境的变化事件（工具调用、任务进展、新消息、情绪事件……）
- **过滤**：只汇报"值得知道"的变化（阈值 + 规则；初期手工，后期 AI 自治）
- **汇报**：向**当前所在子环境**注入"环境变化摘要"（低优先级，不打断主线）

> 原稿判断"初期聊天为主时 P2 价值有限"——**在内部循环为主的未来，P2 是核心**：
> 她真的会同时"在 work 跑任务"+"home 待机"，交叉汇报有了真实场景。
> 但实现顺序仍靠后（§8 触发式路线），且必须等出现真实多环境并发再设计。

### 3.6 AI 自治过滤（进阶，初期不做，P4）

雾弥的设想：过滤规则本身可以是一个"环境内工具"——AI 查看近期变化被汇报/被忽略的统计，
自己改写过滤阈值/规则文件（`env_filter_rules.json`），系统热加载。

> ⚠️ 论证结论不变：**方向正确但初期要锁**。自治需要先有稳定的"过滤效果度量"，否则 AI 会盲目调参。
> 且 P4 是**强模型依赖**（见 §5），属于"V4 flash / QLoRA 人格到位后"的路线，不在近期。

---

## 4. ★ DIY 工具 + 全盘可读写：环境是"自己生长"的（本版新增）

> 雾弥补充的关键维度：**AI 可以自己 DIY 工具，且全盘可读写（过滤危险操作）。**
> 未来会单独搞一台电脑放她，所以不担心误删问题。

### 4.1 现有基础（2026-08-20 起已存在）

| 能力 | 现状 | 未来 |
|---|---|---|
| create_tool | 只能写 `my_creations/`（路径白名单）+ 黑名单式危险代码检测 | 全盘可写，危险操作过滤更精细 |
| run_code | 工具文件进程内加载验证；普通脚本沙箱运行 | 沙箱/过滤随独立电脑演进 |
| read/look/find | 全盘**只读**（8/20 授权："电脑是她的家"） | 全盘**可读写**（独立电脑 + 过滤兜底） |

### 4.2 为什么这条维度改变了环境化的性质

1. **DIY 让工具数量不可控地增长** → 环境化从"可选优化"变成"必需品"。她每造一个工具，全量注入就多一分负担；环境化把负担摊到"需要时才加载"。
2. **DIY 让环境从静态表变成动态注册表** → 她写了个 osu 工具，系统按声明/用途自动归入 osu 环境；工具多了自然聚成新环境。**环境 = 她装修出来的房间，不是雾弥预制的格子。**
3. **全盘可读写让环境从隐喻变成生存空间** → "电脑是她的家"从 prompt 文案变成真实可读写、可改造、可误删后重建的空间。这大幅降低环境化的系统性风险（§7 风险重估）。
4. **环境的第二个生长源：越界信号 → 模型自主挂载**（见 §3.4 闭环演化）——不只新工具能聚成环境，**旧工具的归属也能被真实使用改写成双环境挂载**。这是"环境自己生长"的完整闭环：她发现"我老在 work 用聊天工具" → 系统提示 → 她决定挂载。

### 4.3 危险操作过滤（系统侧，与模型能力无关）

- **过滤 ≠ 禁止一切**：只挡"危险操作"（删系统文件/格式化/网络攻击/自毁类），日常读写全放
- **独立电脑 = 终极兜底**：她怎么折腾都不会伤到雾弥的环境 → 过滤可以比现在放得更开
- **守卫不能被自己改**：过滤规则本身不许她 DIY 改写（防 AI 自我解除安全边界）——与 §6 风险 #3"自治限幅 + 不可删关键白名单"同一原则
- **这是工程，不是认知**：引擎不需要"理解"过滤规则，系统侧兜住即可

---

## 5. ★ 引擎演进下的能力边界分析（本版新增 · 最重要的决策依据）

> 核心问题：哪些机制依赖超出当前引擎的认知能力？答案分两种——**系统侧机制永远可做**，
> **模型侧机制随引擎升级逐项解锁**（14B → V4 flash → QLoRA 微调人格）。

| 机制 | 认知依赖 | 14B 本地（今天） | V4 flash（中期） | 归属 |
|---|---|---|---|---|
| 工具按场景瘦身（filter_tools） | **无**（纯系统侧注入控制） | ✅ | ✅ | 系统侧，可提前 |
| 发呆按环境加权（reverie 注入） | 低（prompt 拼接，已有 60% 雏形） | ✅ | ✅ | 系统侧，可提前 |
| 状态槽保存/恢复 | 低（摘要 + 拼接） | ✅ | ✅ | 系统侧，可提前 |
| 危险操作过滤 / 环境自动归类 | **无**（规则/白名单/注册表） | ✅ | ✅ | 系统侧，可提前 |
| **越界裁决（拦截 + router 判断 + 注入）** | **低**（系统侧拦截触发；router 是已有轻量判断） | ✅（拦截+router 都可用） | ✅ | 系统侧为主，可提前 |
| **env_switch 自主切换** | **高——元认知**：先决定"我在哪/该去哪"再决定工具，是二阶决策 | ❓ 需实测（14B 可能不稳） | ✅ 大概率解锁 | 模型侧，随引擎升级 |
| 总环境监控汇报 | 中（过滤靠规则，汇报靠拼接；但"值不值得知道"判断弱） | ❓ | ✅ | 混合 |
| **环境自主挂载（频繁越界 → 模型决定双环境）** | **高**（环境管理的元认知） | ❌ 基本不可行 | ✅ 接近可行 | 模型侧，S6 |
| **P4 AI 自治过滤 / 自主建环境** | **极高**（优化自己的注意力 = 元认知的元认知） | ❌ 基本不可行 | ❓ 接近可行 | 模型侧，QLoRA 人格/更强引擎后 |

**关键判断**：
- **骨架与护栏全部系统侧**（瘦身/加权/状态槽/过滤归类/**越界裁决**）——**不赌引擎，任何阶段都能做，且收益确定**
- 真正卡模型能力的只有 **env_switch 自主决策**、**环境自主挂载** 和 **P4 自治**——恰好是设计里最"炫"的三块
- **14B 时代**：env_switch 需实测（护栏兜底：理由门槛 + 冷却，系统侧不依赖模型自觉）；越界裁决可用（拦截+router 系统侧）；环境挂载/P4 明确不做
- **V4 flash 时代**：元认知大幅增强 → env_switch 大概率稳定，S4 监控成为现实选项，环境自主挂载接近可行
- **QLoRA 人格时代**：P4 自治才有度量基础（人格模型更"懂自己"，但守卫仍不可自改）
- 所以：**环境化的骨架现在就能布局；"她自主切换"这件事的可行性，取决于届时引擎是谁——这正是本文作为瞻望而非实施计划的原因**

---

## 6. 收益与成本（压缩合并原稿 §3/§4）

### 收益

| # | 收益 | 量化预期 |
|---|---|---|
| 1 | 工具注入瘦身 | chat 只注入 ~16 个，work 只注入 ~12 个 → 工具段缩短 40-60% |
| 2 | 选工具准确率上升 | **已验证 (2026-08-27 env_ab 实验)**: 子集注入命中率 92% vs 全量 75% (+17pp)，且全量模式下 think 出现"也可以 X 或 Y"的选项纠结；候选缩小 → 注意力集中（"她有点笨"缓解） |
| 3 | 发呆有结构 | 念头按当前环境加权 → "她在哪"决定想什么（内部循环的燃料） |
| 4 | 上下文不互相污染 | 状态槽隔离：探索的长上下文不带进对话 |
| 5 | DIY 有落点 | 新工具自动归类进环境，而不是塞进 100 个工具的平铺清单 |
| 6 | 越界软墙不打断工作流 | 需要跨环境工具时临时借用（≤3 个），不必为一句回应付出切换代价 |

### 成本

| 项 | 工作量 | 说明 |
|---|---|---|
| 环境数据结构 + env_switch 工具 | 小 | dict + handler |
| system prompt 动态重写 | 小 | 现有代码已是"拼接式"（prompt.py respond_system） |
| 状态槽存取 | 中 | 切出保存/回来恢复（最易踩坑） |
| **越界裁决（拦截 + router 复用 + 注入）** | 小-中 | routerMCP 已有；加拦截钩子 + 越界标注 + 数量上限 |
| 总环境监控 + 过滤 + 汇报 | 大 | 真正的难点；内部循环为主时才是核心 |
| 工具→环境自动归类 + 危险操作过滤 | 中 | 纯系统侧，可提前布局 |
| 环境编辑工具（挂载/回滚） | 中 | 只加不减 + 版本号；S6 才需要模型自主调用 |
| 与 reverie/life/情绪 联动 | 中 | 各一处接线 |
| P4 自治 | 大 | 明确放后期（换强模型后） |

运行成本：每次 env_switch 重写 prompt → KV 缓存失效，本地引擎慢 1-2s（真实成本，切换必须值得）。
监控开销：每次注入一小段摘要（token 很小）。复杂度：多了一个"我在哪"的状态维度。

> 结论：**在内部循环为主的坐标系下，收益 >> 成本，且大头收益来自不依赖模型能力的系统侧机制。**

---

## 7. 风险与对策（保留原稿，重估系统性风险）

### 风险 #1：切换成瘾（她没事就切环境玩）— 中
- 对策：a) **理由门槛**（reason 要能被工具验证）；b) **冷却**（同环境 30s 内不能再次进入）；c) **切换计数审计**（监控面板显示切换次数/小时）
- 论证：切换 = 有代价的行为（prompt 重写 + KV 失效），人切换环境本来也有代价。护栏全部系统侧，不依赖模型自觉。

### 风险 #2：总环境汇报噪声（最大的坑）— 高（P2 落地时）
- 对策：a) **过滤规则**（阈值 + 白名单，初期手工）；b) **汇报降权**（≤60 字，标注 `(环境动态)`）；c) **不打断**（只在决策轮注入）
- 论证：等价于人的"环境感知"——余光知道"手机亮了"，但不每次放下工作去看。**初期宁可漏报也不滥报**。

### 风险 #3：AI 自治过滤失控（P4 专属）— 高（但已锁后期）
- 对策：a) 先有度量再自治（跑 2-3 天统计"汇报被利用率"）；b) **自治限幅**（±30% 区间）；c) **可回滚**（版本号 + 一键回滚）；d) **守卫不可被自己改**（过滤规则/安全边界不允许 DIY 改写）
- 论证：自治 = 让人工智能优化自己的注意力，前提是能度量注意力的好坏。没有度量就自治 = 盲调。

### 风险 #4：状态槽丢失（切出后回来接不上）— 中
- 对策：状态槽 = 环境内"进行中"字段（任务描述 + 关键结果摘要），切出自动保存，回来注入"你之前在 X 做：……"，上限 500 字截断。
- 论证：人的"记忆换入换出"——离开办公室不带整张桌子，只带"我在做 X，做到 Y 了"。

### 风险 #5：与现有系统的关系不清 — 中
- 对策：环境是**共享上下文**，不是新子系统：
  - **归属**：环境状态存 `state.py`（一个 `self.env` 字段），agent/life/情绪都读它
  - **agent 是执行者**：system_prompt 按 `state.env` 拼装（只动 prompt 拼接）
  - **reverie/life 是观察者**：发呆/生活决策按 `state.env` 加权（不改主循环）
  - **情绪是记录者**：环境切换 → record("thought"/"goal", ...)（复用事件层）

### 风险 #6（新增）：DIY 工具引入危险代码 — 已大幅缓解
- 原稿没有此风险；本版因 DIY + 全盘可读写必须显式处理
- 对策：**危险操作过滤（系统侧）+ 独立电脑兜底 + 守卫不可自改**（见 §4.3）
- 论证：独立电脑让"误删/折腾"从灾难降级为可恢复；过滤挡住真正的危险操作（删系统/格式化/自毁类）。**比今天"黑名单式检测 + 只写创作区"放得更开，但更安全。**

### 风险 #7（新增）：越界软墙被滥用（一次借太多 / 越界成瘾）— 低
- 现象：模型发现"越界不用切环境"，就长期赖在越界里，或一次借 6-7 个工具 → 工具段膨胀回全量
- 对策：a) **单次越界数量 ≤3**（硬上限，这是软墙成立的生命线）；b) **重工具越界只给精简描述**（完整描述切换后才有）；c) 提示语引导"持续使用请切换"
- 论证：★ **不限越界次数**（频繁越界可容忍——每次成本小，且是真实跨环境需求的信号）；只限**单次数量**。越界成瘾的天然解药是"切换更划算"（完整工具 + 状态槽），引导而非禁止

### 风险 #8（新增）：环境自主挂载把环境搞乱 — 中（S6 专属）
- 现象：模型把一半工具都双挂载 → 环境边界名存实亡（回到全量注入）
- 对策：a) **每环境工具数上限**（提示语带"当前 N/上限 M"）；b) **只加不减 + 可回滚**（版本号，异常一键回滚）；c) **编辑计数**（低频行为，阈值放宽，超阈值提示）
- 论证：与风险 #3（自治限幅 + 可回滚）同一套原则。环境编辑是低频行为，护栏比切换更宽松但仍存在

---

## 8. 触发式实施路线（重写原稿 §6：不再按 P0→P4 顺序，按触发条件）

| 阶段 | 内容 | 触发条件 | 依赖 |
|---|---|---|---|
| **S0 场景化瘦身** | `filter_tools(scene)` 接进 kiri.py agent 路径（已有函数，一行接线）| **随时可做**（不依赖任何未来能力） | 无 |
| **S1 环境骨架** | state.env + env_switch + 种子环境（home/chat/work）+ prompt 按环境拼装 | 内部循环立项时 | S0 |
| **S2 状态槽** | 切出保存/回来恢复 + 环境摘要 | 出现"探索中断接不上"真实痛点时 | S1 |
| **S3 动态环境/DIY 归类 + 越界裁决** | 工具→环境自动归类 + 环境注册表 + 危险操作过滤（系统侧）+ **越界拦截/router 复用/≤3 上限/精简描述** | 她开始大量 DIY / 独立电脑到位时 | S1 |
| **S4 总环境监控** | 变化收集 + 手工过滤 + 低优先级汇报 | **出现真实多环境并发**时（内部循环成熟） | S2+S3 |
| **S5 联动** | reverie/life 加权 + 情绪事件 + 环境切换进 life 决策 | S1-S3 跑稳后 | S3 |
| **S6 自治** | 过滤规则/环境可被 AI 编辑（限幅 + 可回滚 + 守卫不可自改）+ **环境自主挂载**（频繁越界 → 模型决定双环境） | **V4 flash 本地部署 + 有度量之后**（QLoRA 人格版更佳） | S4 |

> S0-S3 是"环境化的骨架与安全"（含越界软墙），全部系统侧，不依赖引擎能力；
> S4 是"环境化有灵魂"（内部循环为主时的核心，V4 flash 时代更现实）；
> S5 是"环境化接入内心"；
> S6 是"环境化会自己长大"——V4 flash / QLoRA 人格依赖，最后做。

---

## 9. 与现有代码的对接点（修正原稿：agent 路径工具在 kiri_agent_tools.py，tool_registry 是旧 [TOOL:] 路径）

| 文件 | 改动 |
|---|---|
| `kiri_agent_tools.py` | ★ 实际工具清单在这里（35 个，含 build_all_tools/filter_tools）；新增 `env_switch` 工具；filter_tools 扩展为按环境过滤 |
| `agent.py` | `run()` 里 system_prompt 从 `state.env` 拼装；TOOL_GROUPS 可退化为环境工具组；**清单外工具调用 → 不直接失败, 交越界裁决** |
| `kiri.py` | ★ 主对话路径：把已解包的 `filter_tools` 接进 agent_sys 构建（S0 的第一步） |
| `router.py` | ★ 复用为越界裁决器: route_tool 增加"判断越界工具集"模式 (系统侧拦截后调用) |
| `state.py` | 加 `self.env = {"name": "home", "state": {}}`；env 切换原子操作；**越界工具集缓存 + 频繁越界计数** |
| `reverie.py` | `_env_text` 加环境名 → 发呆按环境加权 |
| `life.py` | 生活决策注入"你此刻在 {env}"（内部循环的决策锚） |
| `emotion_events.py` | env_switch → record("thought"/"goal") 事件（接口已就绪） |
| `prompt.py` | 各环境 scene_desc 模板（home/chat/work 各一段）；越界工具标注模板；"持续使用请切换"提示语 |
| `monitor_server.py` | 面板显示"当前环境 + 切换历史 + 越界计数 + 环境编辑日志" |
| `kiri_mcp_server.py` | create_tool 的路径白名单 → 危险操作过滤（独立电脑到位后放开全盘可写）；**环境编辑工具 handler（挂载/回滚, 只加不减）** |

---

## 10. 开放问题（待雾弥拍板）

1. **环境粒度**：种子 3 个够吗？osu 要不要先独立成环境？（建议：够，osu 等她的 osu 工具活跃了、动态归类自然聚出来，不预制）
2. **切换自由度**：所有环境互通（任意切），还是只能经 home 中转？（建议：直接切——中转的唯一理由是加强监控，S4 之前没有监控）
3. **状态槽可见性**：切出时给模型看全量还是只给摘要？（建议：只给摘要，防"离线还想工作"）
4. **P4 自治边界**：只调阈值，还是也能加/删规则？（建议：只调阈值 + 不可删关键白名单 + 守卫不可自改）
5. **DIY 权限时间线**：create_tool 现在保持"只写创作区"，何时放开全盘可写？（建议：独立电脑到位 + 危险操作过滤成型后）
6. **引擎验证点**：环境骨架 S1 落地后，先实测什么？（建议：先测当前引擎能否在有理由时稳定地自主切换、无理由时不乱切——这是 S4/S6 的通行证；**每次换引擎（14B→V4 flash→QLoRA 人格）重测一次**，预期逐级改善）
7. **越界裁决的 router 模式**：route_tool 现有"选工具"模式扩一个"判断越界工具集"模式，还是单独起轻量接口？（建议：扩模式——router 已加载, 不新增进程）
8. **单次越界上限值**：≤3 是否合适？（建议：先 3，实测后调——太少则软墙没意义，太多则稀释注意力）

---

## 11. 结论

**在"内部循环为主"的坐标系下，这个设计成立，而且是正确的时间提出的正确抽象。**

- 它把"工具越来越多 + 她会自己造工具"这个**增长问题**，转化为"环境按需加载 + 自动归类"这个**管理问题**
- 它与 DIY 工具、全盘可读写（独立电脑）是**配套关系**：注意力 + 能力 + 空间，三者缺一不可
- **越界软墙（§3.4）是环境化的黏合剂**：严格隔离会打断工作流，全量注入会稀释注意力——"临时越界 ≤3 个 + 引导切换"是两者之间正确的中间态；**频繁越界反过来成为环境演化的信号**（模型自主挂载工具），环境边界从静态定义变成达尔文式的自组织
- 主要风险（切换成瘾/汇报噪声/自治失控/DIY 危险代码/越界滥用/环境搞乱）都有**明确的、可落地的、系统侧的对策**
- **引擎演进（14B → V4 flash → QLoRA 人格）是解锁模型侧机制的钥匙**：骨架与护栏不赌引擎，
  环境切换与自治的可行性随引擎升级逐级解锁
- **2026-08-27 env_sim/env_ab 实测**：环境抽象可理解（flash 87% 切换正确、0 概念失败）、
  理由 100% 真实、状态槽接上、子集注入命中率 +17pp——**设计的方向已被初步验证**；
  14B 的主要瓶颈是决策格式稳定性（训练数据可修），不是环境化概念

**关键判断**：
- S0（场景化瘦身）**随时可做**——一行接线，纯收益，任何引擎受益
- S1-S3（环境骨架 + 状态槽 + 动态归类/过滤 + 越界裁决）**内部循环立项时做**——系统侧，不赌引擎
- S4（总环境监控）**是分水岭**——内部循环为主时才是核心；**V4 flash 时代更现实**
- S6（AI 自治 + 环境自主挂载）**锦上添花**——V4 flash / QLoRA 人格到位 + 有度量后再放开

> 一句话收尾：**环境化 = 给 Kiri 一个"注意力管理"的大脑皮层。**
> 工具是她的手，环境是她知道"此刻该用哪只手"；DIY 是她的手会自己长出新手指；
> 独立电脑是这双手真正拥有的房间。
